export declare class TakeIterator<T> implements Iterator<T> {
    private source;
    private limit;
    private i;
    constructor(source: Iterator<T>, limit: number);
    next(): IteratorResult<T>;
}
